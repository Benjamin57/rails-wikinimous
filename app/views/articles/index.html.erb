<div class="container">
  <div class="row">
    <div class="col-sm-8 col-sm-offset-2">
      <% @articles.each do |article| %>
        <article>
         <h3><%= article.title %></h3>
         <p><%= article.content %></p>
        </article>
      <% end %>
    </div>
  </div>
</div>
